public class Blitzle extends ElectricPokemon implements Evolution{
    protected Blitzle(int lifePoints, int maxHp, int maxAp, AttackMove attackMove) {
        super(lifePoints, maxHp, maxAp, attackMove);
    }

    @Override
    public Pokemon evolve() {
        return null;
    }
}
