public class Electabuzz extends ElectricPokemon implements Evolution{
    protected Electabuzz(int lifePoints, int maxHp, int maxAp, AttackMove attackMove) {
        super(lifePoints, maxHp, maxAp, attackMove);
    }

    @Override
    public Pokemon evolve() {
        return null;
    }
}
